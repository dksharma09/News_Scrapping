Virtual env

# python -m venv env
# env\Scripts\activate

github

1) git config --global user.name '***'
2) git config --global user.email '***'
3) git init
4  git add .
5) git commit -m "first commit"
6) git branch -M main
7) git remote add origin https://token@repository_link
8) git remote set-url origin https:// token@repository_link
9) git push -u origin main

